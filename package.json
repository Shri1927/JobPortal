{
  "name": "job-portal",
  "version": "1.0.0",
  "description": "\"a job searching platform\"",
  "main": "index.js",
  "scripts": {
     "build": "npm install --include=dev && npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "add-demo-data": "npm run add-demo-data --prefix backend"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
